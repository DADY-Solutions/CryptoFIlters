<Page 
  loaded="onLoaded"
  unloaded="onUnloaded">
  <StackLayout>
    <Label text="{{'Crypto Filter: ' + cryptoFilter}}" />
    <TextField
      text="{{cryptoFilter}}"
      hint="Crypto..."
      onFocus="onFocus"
      onBlur="onBlur"
    />
  </StackLayout>
</Page>